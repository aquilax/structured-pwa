{
  "name": "appendix-cloudflare",
  "version": "1.0.0",
  "description": "Native Cloudflare Worker replication sync server for structured-pwa using TypeScript and D1",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:init": "wrangler d1 execute appendix-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260906.1",
    "typescript": "^5.7.3",
    "wrangler": "^4.129.0"
  }
}
